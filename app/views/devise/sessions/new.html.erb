<div class="clearfix"></div>
<div class="container_fullwidth sign-up-page">
  <%= render "shared/banner_page", title: t("shared.sign_up.login"), breadscrumb: [{url: "/", title: t("layouts.header.home")}, {url: "/users/sign_in", title: t("shared.sign_up.login")}] %>
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-xs-12 col-md-offset-3">
        <div class="table">
          <div class="table-cell">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>
            <br />
            <div class="field text-left">
              <%= f.label :email, t("shared.sign_up.email") %>:<br />
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control"%>
            </div>
            <br />
            <div class="field text-left">
              <%= f.label :password, t("shared.sign_up.password") %>:<br />
              <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
            </div>
            <br />
            <div class="actions text-center">
              <%= f.submit t("shared.sign_up.login"), class: "btn btn-primary" %>
            </div>
            <% end %>
            <br />
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>